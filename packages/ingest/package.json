{
  "name": "@twitch-golf/ingest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reads channel chat, parses swing commands, forwards them to the EBS.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@twitch-golf/shared": "*",
    "tmi.js": "^1.8.5"
  },
  "devDependencies": {
    "@twitch-golf/ebs": "*",
    "tsx": "^4.19.2"
  }
}
